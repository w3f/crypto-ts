import { should } from 'chai';

import { crypto } from '../src/';

should();


describe('crypto', () => {
    it('Test crypto', () => {
        const c = crypto();

        //((): void => { logger.info('hello world') }).should.not.throw();

    });
});
